describe('Check', () => {
  it('should check the king by rook', () => {
  });

  it('should check the king by queen', () => {
  });

  it('should check the king by bishop', () => {
  });

  it('should not check the king if there is no attack', () => {
  });

  it('should not check the king if blocked by another piece', () => {
  });

  it('should allow the king to move out of check', () => {
  });

  describe('Check - No escape', () => {
    it('should not allow the king to escape check', () => {
    });
  });
  
});
