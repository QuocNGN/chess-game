describe('Pawn Capture', () => {
  it('should capture a piece diagonally', () => {
  });

  it('should not capture a piece if not moving diagonally', () => {
  });

  it('should not capture a friendly piece', () => {
  });
});

describe('Knight Capture', () => {
  it('should capture a piece by moving in an "L" shape', () => {
  });

  it('should not capture a piece outside of knight movement', () => {
  });

  it('should not capture a friendly piece', () => {
  });
});

describe('Bishop Capture', () => {
  it('should capture a piece diagonally', () => {
  });

  it('should not capture a piece if blocked by a friendly piece', () => {
  });

  it('should not capture a friendly piece', () => {
  });
});

describe('Rook Capture', () => {
  it('should capture a piece horizontally', () => {
  });

  it('should capture a piece vertically', () => {
  });

  it('should not capture a piece if blocked by a friendly piece', () => {
  });

  it('should not capture a friendly piece', () => {
  });
});

describe('Queen Capture', () => {
  it('should capture a piece horizontally', () => {
  });

  it('should capture a piece vertically', () => {
  });

  it('should capture a piece diagonally', () => {
  });

  it('should not capture a piece if blocked by a friendly piece', () => {
  });

  it('should not capture a friendly piece', () => {
  });
});

describe('King Capture', () => {
  it('should capture a piece one step away', () => {
  });

  it('should not capture a piece more than one step away', () => {
  });

  it('should not capture a friendly piece', () => {
  });
});
